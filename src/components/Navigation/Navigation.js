import React from 'react';
import css from './Navigation.module.scss';

import { Link } from '@reach/router';

export const Navigation = (props) => {
  return <nav className={css.Navigation}></nav>;
};
